#pragma once

// include
#include <iostream>
#include <string>
#include <sstream>
// using namespace
using namespace std;

enum Color
{
    RED,
    GREEN,
    YELLOW,
    BLUE,
    MAGENTA,
    CYAN,
    WHITE
};

const std::string RESET = "\033[0m";
const std::string COLORS[] = {
    "\033[31m", // RED
    "\033[32m", // GREEN
    "\033[33m", // YELLOW
    "\033[34m", // BLUE
    "\033[35m", // MAGENTA
    "\033[36m", // CYAN
    "\033[37m"  // WHITE
};

// function
template <typename T>
void println(const T &message, const Color &color);
template <typename T>
void println(const T &message);
template <typename T>
void print(const T &message, const Color &color);
template <typename T>
void print(const T &message);
template <typename T>
string convertString(const T &veriable);